/:
  controller: default
  action: default

/test/yves:
  controller: default
  action: default


/ajout-utilisateur:
  controller: "user"
  action: "add"

/utilisateur:
  controller: "user"
  action: "default"

/se-connecter:
  controller: "user"
  action: "login"

/s-inscrire:
  controller: "user"
  action: "register"

/mot-de-passe-oublie:
  controller: "user"
  action: "forgotpwd"


/phps2:
  controller: "user"
  action: "get"

/phps2-json:
  controller: "user"
  action: "json"

/phps2-error:
  controller: "error"
  action: "default"









